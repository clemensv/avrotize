name: Upload Python Package and VS Code Extension 

on:
  push:
    branches: [ master ]
    tags:
      - 'v*'
  pull_request:
    branches: [ master ]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        test-group:
          - name: "Avro to C++/C#"
            pattern: "test_avrotocpp.py test_avrotocsharp.py"
          - name: "Avro to Go/Java/JS"
            pattern: "test_avrotogo.py test_avrotojava.py test_avrotojs.py"
          - name: "Avro to Python/Rust/TS"
            pattern: "test_avrotopython.py test_avrotorust.py test_avrotots.py"
          - name: "Avro to Other Formats"
            pattern: "test_avrotodatapackage.py test_avrotodb.py test_avrotographql.py test_avrotoiceberg.py test_avrotojsons.py test_avrotojstruct.py test_avrotokusto.py test_avrotomd.py test_avrotoparquet.py test_avrotoproto.py test_avrotoxsd.py"
          - name: "Structure to C++/C#"
            pattern: "test_structuretocpp.py test_structuretocsharp.py"
          - name: "Structure to Go/Python"
            pattern: "test_structuretogo.py test_structuretopython.py"
          - name: "Structure to Rust/TS"
            pattern: "test_structuretorust.py test_structuretots.py"
          - name: "Structure to Other Formats"
            pattern: "test_structuretocsv.py test_structuretodatapackage.py test_structuretoiceberg.py test_structuretojsons.py test_structuretomd.py test_structuretoproto.py test_structuretoxsd.py"
          - name: "Format to Avro"
            pattern: "test_asn1toavro.py test_jsontoavro.py test_jstructtoavro.py test_kstructtoavro.py test_parquettoavro.py test_prototoavro.py test_xsdtoavro.py"
          - name: "JSON Structure & Main"
            pattern: "test_jsonstostructure.py test_main.py test_struct_to_schema_references.py"
    name: "Tests: ${{ matrix.test-group.name }}"
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      with:
        submodules: recursive
    - name: Set up .NET SDK 9.0
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 9.0
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        distribution: 'microsoft' 
        java-version: '21'
    - name: Setup Maven Action
      uses: s4u/setup-maven-action@v1.12.0
      with:
        java-version: 21
        maven-version: '3.9.6'
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.10.12
    # we need to build the package for generating the _version.py file that isn't checked in
    - name: Install dependencies and build package
      run: |
        python -m pip install --upgrade pip build wheel
        python -m pip install pylint
        python -m pip install -r requirements.txt
        python -m build --sdist --wheel --outdir dist
    - name: Install dependencies
      run: pip install pytest pytest-md pytest-emoji
    - name: Run pytest for ${{ matrix.test-group.name }}
      uses: pavelzw/pytest-action@v2
      with:
        verbose: true
        emoji: true
        job-summary: false
        click-to-expand: true
        report-title: 'Test Report: ${{ matrix.test-group.name }}'
        custom-pytest: 'pytest ${{ matrix.test-group.pattern }}'

  deploy:
    needs: test
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
      with:
        submodules: recursive
    - name: Set up Python
      uses: actions/setup-python@v3
      with:
        python-version: '3.x'
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install build
    - name: Build avrotize package
      run: python -m build
    - name: Build structurize package
      run: |
        export SETUPTOOLS_SCM_PRETEND_VERSION="${GITHUB_REF#refs/tags/v}"
        cd structurize
        bash build.sh
      env:
        GITHUB_REF: ${{ github.ref }}
    - name: Update VS Code Extension Code with commands.json
      run: |
        python tools/editvscodeext.py --extension-root ${{ github.workspace }}/vscode/avrotize --commands ${{ github.workspace }}/avrotize/commands.json
    - name: Install Node and NPM
      uses: actions/setup-node@v2
      with:
        node-version: '20'
    - name: Set up dependencies for VS Code Extension
      run: |
        cd ${{ github.workspace }}/vscode/avrotize
        npm install
    - name: Publish VS Code Extension
      run: |
        cd ${{ github.workspace }}/vscode/avrotize
        npm run deploy
      env:
        VSCE_PAT: ${{ secrets.VSCE_PAT }}
    - name: Publish avrotize to PyPI
      uses: pypa/gh-action-pypi-publish@27b31702a0e7fc50959f5ad993c78deac1bdfc29
      with:
        user: __token__
        password: ${{ secrets.PYPI_API_KEY }}
    - name: Publish structurize to PyPI
      uses: pypa/gh-action-pypi-publish@27b31702a0e7fc50959f5ad993c78deac1bdfc29
      with:
        user: __token__
        password: ${{ secrets.PYPI_API_KEY }}
        packages_dir: structurize/dist/